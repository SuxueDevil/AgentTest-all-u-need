# 5.17 开发计划

## 当前进度概览
前端 Phase 5 优化 — 全面排查出 20+ 问题，按 Bug → 错误处理 → 加载/空状态 → 响应式 → 死代码清理顺序推进

## 排查结果摘要

### Bug（运行时）
- [ ] ComparisonPage 雷达图用了 xAxis/yAxis 而非 radar.indicator，图表无法正确渲染
- [ ] ComparisonPage agent.color 是 hex 字符串，areaStyle 用 rgb → rgba 替换无效
- [ ] ConfirmDialog 确认按钮固定红色，用于非破坏性操作时误导用户
- [ ] QuestionListPage onTypeChange 切回 multi 时 turns 数据被丢弃
- [ ] App.vue useAppStore() 调了但从未使用

### 错误处理缺失
- [ ] 4 个 Store 全部没有 error 状态暴露
- [ ] 6 个页面没有错误 UI
- [ ] handleApiError 工具函数从未被调用

### 加载/空状态缺失
- [ ] 6 个页面无 LoadingSpinner：Dashboard/Dataset/Report/System/Comparison/EvaluationDetail
- [ ] LoadingSpinner 组件存在但从未被使用
- [ ] EmptyState 组件全局注册但从未被使用

### 死代码/配置问题
- [ ] @composables 和 @hooks 别名目录不存在
- [ ] vue-echarts 依赖未使用
- [ ] 401 跳转 /login 但无 /login 路由

## 已完成事项
- [x] 后端: QuestionGenerator 参数 DTO 化
- [x] 后端: Spring AI 1.1.2 → 1.1.6
- [x] 前端全面代码审查，输出 20+ 问题清单

## API / 接口清单
无新增接口

## 阻塞 / 风险
- 无
